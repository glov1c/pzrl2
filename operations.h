#pragma once

char* replace(char* buffer, char* substr, char* newstr);
char* deleteStr(char* buffer, char* str, char* newstr);
char* addToStart(char* buffer, char* newstr);
char* addToEnd(char* buffer, char* newstr);
